// Grab the relevent box the user wants to rotate by id
function rotate(boxid) {
    const orderBoxOuter = document.getElementById(boxid);
    // Apply the relvent css to actiavte the roation
    orderBoxOuter.focus()
}